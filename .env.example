# UniPool Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env files to version control

# Google Maps API Keys
GOOGLE_MAPS_API_KEY_IOS=your_ios_api_key_here
GOOGLE_MAPS_API_KEY_ANDROID=your_android_api_key_here

# Backend API URL
API_BASE_URL=https://unibackend.acmvit.in

# Expo SDK 56 beta enables expo/fetch as global fetch by default.
# Keep React Native's fetch until the native SharedObject event crash is fixed upstream.
EXPO_PUBLIC_USE_RN_FETCH=1

# Firebase Configuration (if needed separately from google-services files)
# These are typically handled by the google-services.json and GoogleService-Info.plist files
# on native. The web build needs the Firebase Web app config via EXPO_PUBLIC_* vars:
EXPO_PUBLIC_FIREBASE_API_KEY=your_firebase_web_api_key_here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
EXPO_PUBLIC_FIREBASE_APP_ID=1:your_sender_id:web:your_app_id
