/**
 * Props for GenderSelector component.
 * Currently no props, but defined for scalability.
 */
export interface GenderSelectorProps {
  value: string | null;
  onChange: (value: string) => void;
}
