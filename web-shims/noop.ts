// Empty module used as a web alias target for side-effect-only native
// imports that have no web equivalent (e.g. "expo-sqlite/localStorage/install",
// which polyfills localStorage on native; the browser already has it).
export {};
